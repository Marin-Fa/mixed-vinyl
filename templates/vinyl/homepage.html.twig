{% extends 'base.html.twig' %}

{% block title %}
    Create template
{% endblock %}

{% block body %}
    <h1>{{ title }}</h1>
    {# TODO #}
    <div>
        Tracks
        <ul>
            {% for track in tracks %}
            <li>
                {{ track.song }} - {{ track.artist|upper }}
            </li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}